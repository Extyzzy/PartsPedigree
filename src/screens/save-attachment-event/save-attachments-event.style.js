import { StyleSheet } from 'react-native';

export const saveAttachmentsEventStyle = StyleSheet.create({
    container: {
        padding: 20,
    },
    formBtnsContainer: {
        position: 'absolute',
        bottom: 0,
        left: 0,
    },
    attachmentsContainer: {
        minHeight: 300,
    }
});
